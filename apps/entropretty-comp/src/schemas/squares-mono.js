// Inspired by "Quadrate - Herbert Franke".

import { doDraw } from "./squares-chroma";

export const schema = {
  draw: (c, s) => doDraw(c, s, true),
  name: "Squares (Mono)",
  artist: "ggwpez.gh",
};
